spring:
  datasource:
    url: jdbc:mysql://localhost:3306/catshop?useSSL=false&allowPublicKeyRetrieval=true&useTimezone=true&serverTimezone=UTC
    username: root
    password: 123456
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8090

catshop:
  initialFee: 200.00

